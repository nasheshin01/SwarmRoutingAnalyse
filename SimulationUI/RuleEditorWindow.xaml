<Window x:Class="SimulationUI.RuleEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Simulation"
        xmlns:simulationUi="clr-namespace:SimulationUI"
        Title="Rule Editor" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Rule list -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <ListBox x:Name="RulesList"/>
            <StackPanel Margin="10">
                <Button Content="Add Rule" Click="OnAddRuleButtonClicked"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>